{
  "name": "super-agent-brain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Super Agent brain: pi-agent-core loop running in Termux, talking to the Android body over localhost IPC",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx src/main.ts",
    "mock": "tsx test/mock-body.ts",
    "smoke": "tsx test/smoke.ts",
    "live-glm": "tsx test/live-glm.ts",
    "typecheck": "tsc --noEmit",
    "start": "tsx src/main.ts",
    "sync-vocab": "node --input-type=module -e \"import{copyFileSync}from'node:fs';copyFileSync('../body/common/src/main/resources/commit_boundaries.json','./src/guards/commit_boundaries.json')\""
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "latest",
    "@earendil-works/pi-ai": "latest",
    "typebox": "^1.3.15"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
